# mlp_inference_engine_stm32
The goal is to build an inference engine in C for Multi Layer Perceptrons, that runs directly on a STM32. Networks running on the STM32 should be able to solve problems such as classifying the MNIST-Dataset.
